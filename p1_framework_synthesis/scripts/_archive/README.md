# `_archive/` — Superseded Phase 1 scripts

Historical earlier versions of the build scripts. Replaced by the active scripts in [`..`](..). Retained for provenance only — **do not invoke**.

| File | Replaced by |
|------|-------------|
| `build_review_data.py` | `../build_review_data_done.py` |
