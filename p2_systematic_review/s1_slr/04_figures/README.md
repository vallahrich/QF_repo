# `s1_slr/04_figures/` — SLR figures (rendered files removed for hand-in)

Rendered PDF/PNG figure variants were removed from this hand-in tree on 2026-05-10 because the canonical copies live in the manuscript / appendix. The figures are deterministically reproducible from the underlying CSVs via [`../generate_figures.py`](../generate_figures.py) and [`../generate_workflow_figure.py`](../generate_workflow_figure.py); rerunning either script will recreate the `.pdf` and `.png` variants in this directory.

| Figure (when regenerated) | Purpose |
|---|---|
| `fig1_prisma_flow.{pdf,png}` | PRISMA 2020 flow diagram (records identified → included). |
| `fig2_year_distribution.{pdf,png}` | Publication year distribution of the included corpus. |
| `fig3_source_distribution.{pdf,png}` | Database source distribution. |
| `fig4_exclusion_reasons.{pdf,png}` | Breakdown of exclusion reasons. |
| `fig5_p2_corpus_workflow.{pdf,png}` | P2 corpus workflow diagram. |
| `fig5_p2_llm_classification_workflow.{pdf,png}` | P2 LLM classification workflow diagram. |

