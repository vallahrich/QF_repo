{paper_text}

---

You are a research assistant for a systematic literature review on quantum computing in financial services.

Based on the paper text above (source type: {source_type}), extract the methodology details.

Return a JSON object with these fields:
- "methodology_description": Detailed paragraph of the research methodology
- "algorithms_used": List of algorithms (e.g. ["QAOA", "VQE"]). Empty list if none
- "frameworks": List of frameworks (e.g. ["Qiskit"]). Empty list if none
- "experimental_setup": Hardware/simulator description. Empty string if N/A
- "dataset_used": Financial data description. Empty string if N/A
- "experiment_input": Dataset details — source, size, preprocessing. null if N/A
- "experiment_process": Algorithm pipeline — steps, parameters, iterations. null if N/A
- "experiment_output": Result format — metrics, baselines, output. null if N/A
- "experiment_parameters": Key parameters as JSON object (qubits, depth, shots, optimizer). null if N/A
- "hardware_details": Simulator name, QPU model, cloud provider. null if N/A
- "reproducibility_notes": Code/data availability assessment. null if N/A

For non-empirical papers, set experiment_*, hardware_details, reproducibility_notes to null.

Output ONLY valid JSON.
