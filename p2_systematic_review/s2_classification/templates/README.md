# `s2_classification/templates/` — Per-paper Markdown skeleton

| File | Purpose |
|------|---------|
| `paper_base.md` | Frontmatter + section skeleton that `extract_paper.py` populates for each paper in [`../../output/processed/`](../../output/processed/). |

Edits to this file change the on-disk structure of every classified paper — coordinate with downstream P3 consumers.
