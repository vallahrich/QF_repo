# `p3_thematic_synthesis/problems/` — Historical per-silo scaffolding

Pre-restructure scaffolding for the eight active P3 silos (plus the excluded
`cryptography_security` silo retained for traceability). The active per-silo
working data now lives under `../s1_silo_scoping/<silo>/` and
`../s4_thematic_coding/<silo>/`; this folder is kept only so historical
references in prompts and audit logs continue to resolve.

See [`../P3_AUDIT_STATUS.md`](../P3_AUDIT_STATUS.md) for the audit on which
silos are active vs. archived.