# `problems/cryptography_security/` — Out-of-scope silo (excluded)

PD-08 cryptography is excluded from the active P3/P4 pipeline as out-of-scope
per `shared/config/unified_taxonomy.json` v2.0. This directory exists only to
keep historical references resolvable.

See parent [`../README.md`](../README.md).