## Paper Text

{paper_text}

## Existing Codes

{existing_codes}

## Instructions

You are a research assistant performing a COVERAGE GAP CHECK on thematic codes extracted from the paper above.

Another analyst has already produced the codes listed in "Existing Codes". Your job is to read the paper carefully and identify important concepts, claims, or findings that were MISSED in the existing codes.

## Task

1. Read the PAPER TEXT thoroughly.
2. Review the existing codes to understand what has already been captured.
3. Identify 5-15 significant concepts from the paper that are NOT covered by any existing code.

Focus on these commonly missed areas:
- Specific quantitative results (numbers, percentages, speedups) not yet coded
- Classical baseline details and comparison methodology
- Hardware/simulator constraints and their implications
- Methodological assumptions or simplifications the authors make
- Related work positioning — how this work differs from prior approaches
- Reproducibility gaps (missing code, limited datasets, unreported hyperparameters)
- Unstated limitations that a critical reader would notice
- Quantum circuit specifics (depth, gates, encoding, noise model)

CRITICAL RULES:
1. VERBATIM QUOTES ONLY — copy the EXACT words from the PAPER TEXT for text_span. 10-60 words. Do NOT paraphrase.
2. NO DUPLICATES — do NOT re-code concepts already captured in the existing codes, even with different wording.
3. SCOPE — only code claims from the paper text. Do NOT infer or add external knowledge.
4. FIELD NAMES — use EXACTLY: code_id, code_label, text_span, locator, dimension_hint, rationale.
5. Continue code_id numbering from where existing codes end (e.g., if last existing code is C-048, start at C-049).

## Output Format

Return a JSON object with key "gap_codes" containing an array:

{"gap_codes": [{"code_id": "C-049", "code_label": "Circuit depth scales quadratically with assets", "text_span": "the circuit depth grows as O(n^2) where n is the number of assets", "locator": "Paragraph 12", "dimension_hint": "limitation", "rationale": "Scalability constraint missed in initial coding — critical for practical assessment"}]}

dimension_hint must be one of: objective, method, result, limitation, future_work, cross_cutting, other

If the existing codes are comprehensive and you cannot find meaningful gaps, return: {"gap_codes": []}

You MUST return valid JSON only.
