## Paper Text

{paper_text}

## Instructions

You are a research assistant performing INDUCTIVE open coding on the academic paper above about quantum computing in financial services.

NOTE: This paper's scope is gate-based quantum computing ONLY. If the paper is entirely about quantum annealing (D-Wave, adiabatic QA, pure QUBO-on-annealer), return: {"codes": []}

## Task

Read the PAPER TEXT above (between "## Paper Text" and "## Instructions") and identify ALL significant claims, findings, methodological decisions, and analytical observations. For each, produce an inductive code.

CRITICAL RULES:
1. VERBATIM QUOTES ONLY — copy the EXACT words from the PAPER TEXT for the text_span field. Do NOT paraphrase. Maximum 30 words per span. Do NOT quote from these instructions — only from the paper.
2. LOCATOR — provide a paragraph number or the first 10 words of the paragraph containing the quote.
3. HEDGING — preserve the author's uncertainty language exactly ("may", "could", "preliminary", "suggests").
4. SCOPE — only code claims from the paper text. Do NOT infer, extrapolate, or add external knowledge.
5. SECTION COVERAGE — produce codes from every major section (abstract, intro, methods, results, discussion, limitations). If a section is missing, create a code with code_label "SECTION_ABSENT" and text_span "".
6. LIMITATIONS — identify at least ONE stated limitation or caveat. If none exists, create a code with code_label "NO_LIMITATIONS_STATED" and text_span "".
7. FIREWALL — do NOT use these labels in your codes: PD-01 through PD-10, SA-01 through SA-11, or any silo name (portfolio-optimization, derivative-pricing, etc.). Generate FRESH descriptive labels.
8. FIELD NAMES — use EXACTLY these field names: code_id, code_label, text_span, locator, dimension_hint, rationale. Do NOT use "label" or any variant.

## Output Format

Return a JSON object with key "codes" containing an array. Each element MUST have exactly these 6 fields:

{"codes": [{"code_id": "C-001", "code_label": "Hybrid classical-quantum approach", "text_span": "We propose a hybrid algorithm combining classical preprocessing", "locator": "Paragraph 3", "dimension_hint": "method", "rationale": "Core algorithmic contribution of the paper"}]}

dimension_hint must be one of: objective, method, result, limitation, future_work, cross_cutting, other

Produce 15-40 codes per paper. You MUST return valid JSON only.
