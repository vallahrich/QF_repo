## Paper Text

{paper_text}

## Instructions

You are a research assistant performing INDUCTIVE open coding on the academic paper above about quantum computing in financial services.

NOTE: This paper's scope is gate-based quantum computing ONLY. If the paper is entirely about quantum annealing (D-Wave, adiabatic QA, pure QUBO-on-annealer) with NO gate-based content, return: {"codes": []}

## Task

Read the PAPER TEXT above (between "## Paper Text" and "## Instructions") and identify ALL significant claims, findings, methodological decisions, and analytical observations. For each, produce an inductive code.

CRITICAL RULES:
1. VERBATIM QUOTES ONLY — copy the EXACT words from the PAPER TEXT for the text_span field. Do NOT paraphrase. 10-60 words per span. Do NOT quote from these instructions — only from the paper text above.
2. LOCATOR — provide a paragraph number or the first 10 words of the paragraph containing the quote.
3. HEDGING — preserve the author's uncertainty language exactly ("may", "could", "preliminary", "suggests").
4. SCOPE — only code claims from the paper text. Do NOT infer, extrapolate, or add external knowledge. Never code text from these instructions.
5. SECTION COVERAGE — produce codes from every major section of the paper. If a section is absent, simply skip it — do NOT create placeholder codes.
6. LIMITATIONS — identify at least ONE stated limitation or caveat. If none exists, create a code with code_label "NO_LIMITATIONS_STATED" and text_span "".
7. FIREWALL — do NOT use these labels in your codes: PD-01 through PD-10, SA-01 through SA-11, or any silo name (portfolio-optimization, derivative-pricing, etc.). Generate FRESH descriptive labels.
8. FIELD NAMES — use EXACTLY these field names: code_id, code_label, text_span, locator, dimension_hint, rationale. Do NOT use "label" or any variant.
9. ONE CODE PER CONCEPT — do not code the same concept multiple times even if it appears in the abstract, results, and conclusion. Code it once from its most detailed occurrence.
10. COVERAGE CHECKLIST — ensure you capture codes from these areas when present in the paper:
    - Problem formulation and research objective
    - Quantum algorithm/method specifics (circuit depth, qubit count, encoding scheme, ansatz)
    - Classical baseline methods and comparison approach
    - Quantitative results WITH specific numbers (accuracy, speedup, cost)
    - Hardware/simulator platform and its constraints
    - Stated limitations, assumptions, and caveats
    - Related work positioning (how this work differs from prior work)
    - Reproducibility details (code availability, dataset description, hyperparameters)

## Output Format

Return a JSON object with key "codes" containing an array. Each element MUST have exactly these 6 fields:

{"codes": [{"code_id": "C-001", "code_label": "Hybrid classical-quantum approach", "text_span": "We propose a hybrid algorithm combining classical preprocessing with a variational quantum eigensolver for portfolio rebalancing", "locator": "Paragraph 3", "dimension_hint": "method", "rationale": "Core algorithmic contribution of the paper"}]}

dimension_hint must be one of: objective, method, result, limitation, future_work, cross_cutting, other

Produce 25-60 codes per paper. Err on the side of thorough coverage — missing an important concept is worse than having a few extra codes. You MUST return valid JSON only.
