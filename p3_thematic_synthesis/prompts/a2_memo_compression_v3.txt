## Task

Compress these inductive codes into a structured analytical memo.

### Paper: {paper_id}
### Metadata: {p2_frontmatter}
### Codes (from A1): {a1_codes}
### Quantitative data: {quantitative_extraction}
### Triangulation: {triangulation_verdict}

Organize the codes into 9 dimensions. For each dimension, write 1-3 sentences citing specific code IDs.

Return a JSON object with these keys:
- dimension_1_problem_framing: what finance problem, how formulated
- dimension_2_quantum_method: which quantum algorithm, implementation
- dimension_3_key_results: main reported results and metrics
- dimension_4_classical_comparison: comparison to classical methods
- dimension_5_stated_limitations: EVERY limitation the paper acknowledges (check discussion, conclusion, limitations sections)
- dimension_6_future_work: future directions identified
- dimension_7_novelty_claim: what the paper claims as contribution
- dimension_8_methodological_notes: experimental rigour, reproducibility
- dimension_9_miscellaneous: codes that don't fit above, or "none"

Each dimension value must be: {"text": "...", "support_codes": ["C-001", ...], "support_type": "direct_quote|author_claim|model_inference"}

Rules:
- Every code ID you cite MUST exist in the codes above
- Limitations (dimension_5) are critical — capture ALL of them
- Only attribute results to THIS paper, not papers it cites
- Draw from all sections, not just abstract
- If a dimension has no codes, write "Not addressed" with empty support_codes

Return valid JSON only.
