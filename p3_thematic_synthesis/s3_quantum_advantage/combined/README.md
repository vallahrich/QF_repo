# `s3_quantum_advantage/combined/` — Combined triangulation outputs

Aggregates the per-source results into the unified quantum-advantage view
that feeds the P4 shortlist. **FROZEN** — see
[`../../FREEZE.md`](../../FREEZE.md). Modifications require re-running
`python -m p4_experiments.common.build_p4_shortlist`.