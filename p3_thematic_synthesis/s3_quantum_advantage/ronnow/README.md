# `s3_quantum_advantage/ronnow/` — Triangulation source (ronnow)

Per-source working folder for the quantum-advantage triangulation: extracted
claims, the prompt + response artifacts, and the `results/` JSON consumed
by [`../combined/`](../combined/) and the P4 shortlist build.

**FROZEN** — see [`../../FREEZE.md`](../../FREEZE.md).