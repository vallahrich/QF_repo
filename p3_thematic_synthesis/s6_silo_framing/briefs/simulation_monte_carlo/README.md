# `briefs/simulation_monte_carlo/` — Framing brief (simulation_monte_carlo)

Researcher-facing framing brief for the simulation_monte_carlo silo, plus a `_request/`
subfolder with extraction-request prompts.

See parent [`../README.md`](../README.md).