# `s6_silo_framing/extractions/` — Per-paper extractions for framing

| Subfolder | Purpose |
|-----------|---------|
| `papers/` | Per-paper extraction JSONs used to build the framing briefs. |
| `_raw/` | Raw LLM responses prior to parsing/validation. |
| `_requests/` | Outgoing extraction-request payloads. |

See parent [`../README.md`](../README.md).