{
  "paper_id": "1d715e0cd1aa",
  "silos": [
    "portfolio_optimization",
    "quantum_ml_finance",
    "risk_management"
  ],
  "problem_statement": "The paper frames the financial problem as linking noisy risk/return forecasting to a constrained allocation decision, rather than treating prediction and optimization as separate steps. It also frames the problem as needing traceable, auditable outputs in regulated settings.",
  "problem_statement_quotes": [
    "Financial risk systems usually follow a two-step routine: a model predicts return or risk, and then an optimizer makes a decision such as a portfolio rebalance.",
    "In practice, this split can break under real constraints.",
    "Also, regulated settings need a clear audit trail that links each decision to the exact model state and inputs."
  ],
  "classical_baseline": "The paper treats the classical baseline as a standard ML-to-optimizer workflow, including classical forecasting models followed by classical portfolio optimization methods. In the experiments, it compares against ARIMA, LSTM, GRU, TCN, Transformer, equal-weight, mean-variance with projected gradient, and a classical simulated-annealing QUBO solver.",
  "classical_baseline_quotes": [
    "Financial risk systems usually follow a two-step routine: a model predicts return or risk, and then an optimizer makes a decision such as a portfolio rebalance.",
    "For forecasting, we compare against: ARIMA (return only), LSTM, GRU, TCN, and a small Transformer encoder.",
    "For allocation, we compare against: (a) equal-weight (EW), (b) mean–variance with classical projected gradient (MV-PG) on the same ˆµt, ˆΣt, and (c) a classical mixed-integer heuristic that uses the same binary budget B but solves with simulated annealing (SA-QUBO)."
  ],
  "classical_difficulty": "The paper says the classical difficulty is that the prediction-to-decision split can fail under market shifts and discrete constraints, and that optimization becomes slow as the asset set grows. It also says standard workflows often hide how uncertainty enters the downstream decision and lack traceable outputs.",
  "classical_difficulty_quotes": [
    "the final decision can be unstable when the market shifts, when discrete constraints are added (lot sizes, caps), or when the optimization becomes slow for larger asset sets.",
    "Second, decision optimization becomes expensive when the number of assets, constraints, or discrete choices grows, which pushes teams toward simplified formulations or local heuristics.",
    "Third, enterprise settings require traceable outputs (what model version produced what allocation and why), which is often missing in standard ML-to-optimizer workflows."
  ],
  "business_stakes": "The paper explicitly mentions regulated settings and compliance review as the real-world stakes. It says the audit layer is meant to support verification in regulated settings and to link allocations to the exact model state and time window.",
  "business_stakes_quotes": [
    "Also, regulated settings need a clear audit trail that links each decision to the exact model state and inputs.",
    "This does not change accuracy, but it makes the pipeline traceable in regulated settings: a reviewer can verify that a stored decision was produced by a specific trained model at a specific time using the public key pk.",
    "This step does not change forecasting or allocation outputs; it only provides a verifiable record of what model produced what portfolio at what time."
  ],
  "extraction_confidence": "high",
  "extraction_notes": "Motivation is explicit in abstract and introduction; paper is a hybrid pipeline with clear classical comparison and regulated-setting framing."
}