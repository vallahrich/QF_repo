# `p3_thematic_synthesis/shared/` — Shared P3 prompts and scripts

Cross-stage helpers shared by P3 sub-stages: `prompts/` for prompt fragments
re-used across stages, `scripts/` for utilities (citation graph, indices,
contradiction detection, missing-paper reporting).

See parent [`../README.md`](../README.md).