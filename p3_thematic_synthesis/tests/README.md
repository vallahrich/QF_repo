# `p3_thematic_synthesis/tests/` — Phase 3 release tests

| Test | Purpose |
|------|---------|
| `test_release_invariants.py` | Frozen-output invariants (paper counts, schema, hashes). |
| `test_silo_consistency.py` | Per-silo cross-stage consistency (a1↔b2↔c2). |

Run from repo root: `pytest p3_thematic_synthesis/tests/`.

See parent [`../README.md`](../README.md).