# Canonical Data Namespace

This is the target home for checked canonical input state such as `cohort.json`, `phase3_compare.json`, and `s2_extraction_index.py`.

For now, the authoritative files remain in [../](../) for compatibility with the active pipeline and audits. Do not duplicate JSON state here until the readers and Phase 11 bundle generator are migrated together.