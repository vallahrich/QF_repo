\begin{tabular}{lll}
\toprule
Axis & Invariant & Reason \\
\midrule
logical\_qubits & True & Logical qubit count is a property of the QIR program. Azure RE does not vary it across hardware profiles; tau = full\_logical\_qubits / bare\_logical\_qubits = 1 identically for every (label, profile, eps) cell. Friedman degenerates on constant blocks. \\
t\_count & True & Logical T-gate count is determined by the input algorithm and the rotation-synthesis epsilon, not by the QEC profile. tau is therefore invariant across profiles within a fixed epsilon. \\
t\_depth & True & Logical T-depth is invariant across QEC profiles for the same reason as t\_count: it is fixed by the QIR program and the rotation-synthesis epsilon. \\
\bottomrule
\end{tabular}
