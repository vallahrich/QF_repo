# `canonical/prompts/` — No Active Canonical Prompts

The active canonical Phase 4 pipeline is deterministic and scripted. It does
not use LLM prompt templates to generate Resource Estimator evidence.

This directory is retained only as an explicit marker that there are no active
canonical prompt files. Cross-cutting audit/review prompts, where retained, live
under [`../../prompts/`](../../prompts/) and are not pipeline inputs.