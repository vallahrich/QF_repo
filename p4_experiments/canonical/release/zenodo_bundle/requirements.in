# Phase 7 - Canonical direct dependencies (input to requirements.lock).
# Edit this file when adding/removing a direct dep, then regenerate the
# pinned lockfile via tools/regen_requirements_lock.ps1.

# --- Quantum stack ---
qsharp
qiskit
qiskit-aer
azure-quantum

# --- Scientific stack ---
numpy
scipy
sympy
networkx
matplotlib

# --- Machine learning (classical baselines, datasets) ---
scikit-learn

# --- Validation / IO ---
jsonschema
pydantic

# --- Utilities used by canonical scripts ---
requests
PyYAML
tqdm
