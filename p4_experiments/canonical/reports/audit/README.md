# Canonical Audit Reports

This is the authoritative home for generated `audit_phase*.json` reports and the orchestrator-level `audit_report.json` summary.

The audit implementations live in [../../audits/](../../audits/), and legacy flat audit module paths in [../../](../../) remain compatibility wrappers for old `python -m p4_experiments.canonical.audit_phase*` commands.