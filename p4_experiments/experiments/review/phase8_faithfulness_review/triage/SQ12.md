# Triage: SQ12

**Reviewers (joint):** Vincent Wallerich + Aleix Telesforo  
**Triage date:** 2026-04-25  
**Vincent verdict (first pass):** **DRIFT_MINOR**

---

## Paper

- **Title:** Quantum Machine Learning: Harnessing Quantum Algorithms for Supervised and Unsupervised Learning
- **Paper ID:** `5413b7728054` · **Experiment:** `exp_1` · **Silo:** `quantum-ml-finance` · **Cohort algorithm family:** `quantum-svm`

**Sources to consult:**
- Paper markdown: [p2_systematic_review/output/processed/5413b7728054.md](p2_systematic_review/output/processed/5413b7728054.md)
- Circuit: [p4_experiments/experiments/silos/quantum_ml_finance/5413b7728054__exp_1/circuit.py](p4_experiments/experiments/silos/quantum_ml_finance/5413b7728054__exp_1/circuit.py)
- Instance: [p4_experiments/experiments/silos/quantum_ml_finance/5413b7728054__exp_1/instance.json](p4_experiments/experiments/silos/quantum_ml_finance/5413b7728054__exp_1/instance.json)
- Phase 3 LLM extraction (GPT 5.3 / GPT 5.4-mini, P3 s2_quantitative): [p3_thematic_synthesis/s2_quantitative/output/extractions/5413b7728054.json](p3_thematic_synthesis/s2_quantitative/output/extractions/5413b7728054.json) (experiment_id `exp_1`)
- Vincent's review: [../vincent/SQ12.md](../vincent/SQ12.md)

---

## Disagreement

Vincent's review (first pass) flagged this label as **DRIFT_MINOR** —
i.e. the implemented circuit does not faithfully match what the
Phase 3 LLM extraction (GPT 5.3 / GPT 5.4-mini) recorded the paper's algorithm to be.

**Vincent's verdict summary:** Generic RealAmplitudes ansatz_stretch proxy authored at n=5 for a QSVM paper that reports no circuit details; template-proxy methodology applies but the proxy lacks QSVM-specific feature-map/kernel structure.

## Phase 3 (LLM) extraction — values for this label

**Source:** [p3_thematic_synthesis/s2_quantitative/output/extractions/5413b7728054.json](p3_thematic_synthesis/s2_quantitative/output/extractions/5413b7728054.json) (experiment_id `exp_1`)  
**Models used (per source `extraction_metadata.models_used`):** `gpt-5.3` (synthesis) + `gpt-5.4-mini` (extraction steps).

| Field | Phase 3 value |
|---|---|
| `algorithm_family` | `quantum-svm` |
| `algorithm_variant` | `QSVM` |
| `num_qubits` | _(not extracted in P3 S2)_ |
| `ansatz` (free-text) | _(not extracted in P3 S2)_ |
| `num_layers` | _(not extracted in P3 S2)_ |
| `circuit_depth` | _(not extracted in P3 S2)_ |
| `oracle_structure` | _(not extracted in P3 S2)_ |
| `encoding` | _(not extracted in P3 S2)_ |

_Note: P3 s2_quantitative extracts paper-level algorithm and quantum-resource fields. Circuit-level details that are not in the S2 schema (e.g. `oracle_structure`, `encoding`) appear as _(not extracted in P3 S2)_ above. Vincent's per-check notes below adjudicate those fields from direct paper + circuit reading._

### Vincent's per-check notes

| Check | Result | Note |
|---|---|---|
| Family-faithful | ❌ | Paper claims QSVM (kernel/feature-map based classifier); circuit is a generic variational RealAmplitudes ansatz with compute-uncompute oracle. Same cohort tag (quantum-svm) but the implemented structure is an ansatz-stretch template, not a QSVM kernel circuit. Acceptable under explicit template-proxy methodology since paper provides no feature-map or kernel specifics. |
| Scale-faithful | ✅ | Paper does not state n_qubits (NOT_STATED); proxy uses n=5, which is within the tractable default range and within factor-of-2 of any plausible small-scale QSVM heart-disease demo. |
| Structurally non-trivial | ✅ | Bare circuit is a 5-qubit RealAmplitudes ansatz with 3 reps (linear entanglement, fixed seed); full variant adds ansatz . ansatz^dagger compute-uncompute and measurement. Non-trivial parametrised circuit, not empty. |
| Metadata-consistent | ✅ | label_id, paper_id, silo (quantum-ml-finance), and algorithm_family (quantum-svm) match across instance.json, circuit registration, and v2 extraction. authoring_template=ansatz_stretch matches build_bare's call to ansatz_stretch. paper_claimed fields correctly null since v2 has NOT_STATED. |

### Concerns raised by Vincent

- Paper algorithm is QSVM (kernel-based) but circuit implements a generic variational ansatz, not a QSVM feature-map+kernel-estimation circuit (e.g., ZZFeatureMap + SWAP-test or fidelity kernel).
- Paper has no stated qubit count, depth, feature map, or kernel choice (v2 fidelity_assessment='P'), so the proxy cannot be validated against any quantitative paper claim.
- Cohort inclusion flag in operator_notes: dataset is heart disease (non-finance) — flagged but not blocking for the resource-cost study.

### Recommendations from Vincent

- If a more family-faithful proxy is desired, swap ansatz_stretch for a QSVM kernel-estimation template (e.g., ZZFeatureMap + adjoint feature map for fidelity estimation) at the same n=5 scale.
- Note in cohort metadata that this entry is a template proxy with family_drift=ansatz-vs-kernel so downstream resource aggregation does not over-claim QSVM-specific structure.

---

## DECISION (joint manual review)

```yaml
label_id: SQ12
paper_id: 5413b7728054
experiment_id: exp_1
reviewers: [Vincent Wallerich, Aleix Telesforo]
reviewed_utc: 2026-04-25

# winner: 'phase3' | 'vincent' | 'merge' | 'other'
# action:  'accept_vincent' | 'reauthor_circuit' | 'demote_label' | 'relabel_family' | 'no_change'
decision:
  winner: vincent
  action: no_change
  rationale: |
    Accept Vincent's DRIFT_MINOR assessment.

    Phase 3 is faithful at the paper-algorithm level: the paper identifies Quantum Support Vector Machines (QSVMs) as the relevant supervised-learning method and reports an experimental comparison between classical SVM and QSVM on a Kaggle heart-disease dataset using IBMQ/Qiskit. The paper describes QSVM as a kernel-based classifier that uses quantum kernels and high-dimensional quantum feature spaces for classification.

    However, the paper does not specify a concrete QSVM circuit. It does not provide a feature-map circuit, kernel-estimation circuit, number of qubits, circuit depth, entanglement pattern, shot count, backend details, or gate-level decomposition. Therefore, a faithful circuit-level reproduction is not possible from the paper alone.

    The implemented circuit described in Vincent's review is a generic RealAmplitudes ansatz-stretch proxy with a compute-uncompute structure. This is not structurally QSVM-specific: it does not implement a quantum feature map, an adjoint feature-map fidelity kernel, a SWAP-test kernel estimator, or an HHL/least-squares QSVM construction. Therefore, there is real family/structure drift between the paper's QSVM claim and the implemented circuit body.

    The drift is minor rather than major because the paper itself is underspecified and provides no reproducible circuit template to compare against. The metadata is internally consistent, the label/paper/family tags match, and the proxy is explicitly disclosed as an ansatz_stretch template. The current artifact may remain as a resource-only template proxy, but it should not be interpreted as a family-faithful QSVM kernel circuit.
  evidence_section: >
    PDF page 2, Abstract: the paper states that Quantum Support Vector Machines (QSVMs) and Quantum Neural Networks are used for supervised learning, with QSVMs using quantum kernels and entanglement for classification/regression tasks.
    PDF page 4, Related Work: the paper discusses the Rebentrost et al. quantum SVM formulation and notes phase estimation and matrix inversion in the least-squares SVM setting, but this is presented as background rather than as a fully specified circuit for the paper's experiment.
    PDF page 5, Related Work and QML Techniques: the paper discusses quantum kernel estimators, quantum variational classifiers, and QSVM feature-space methods, confirming the QSVM/kernal-method context but still without specifying a concrete implementation circuit.
    PDF page 6, "Quantum Support Vector Machine": the paper states that QSVM is used for classification, that the experiment uses the Kaggle heart-disease dataset, and that QSVM and SVM were implemented on IBMQ using Qiskit.
    PDF pages 6-7, Figures 2 and 3: the paper reports QSVM accuracy of 97.64 versus SVM accuracy of 94.3, and QSVM execution time of 14 seconds versus SVM execution time of 33 seconds, but gives no circuit-level details.
  remediation:
    template: ansatz_stretch
    n_qubits: 5
    notes: |
      Recommended immediate handling:
      - Keep SQ12 in the cohort as a disclosed template proxy.
      - Mark it as DRIFT_MINOR / template-proxy / not family-structural.
      - Do not treat the current RealAmplitudes circuit as a faithful QSVM kernel-estimation implementation.
      - Retain n_qubits=5 as a tractable default because the paper does not state a concrete qubit count.

      Metadata:
      - No label/paper/experiment metadata correction is required based on Vincent's review.
      - Keep algorithm_family='quantum-svm' at the paper/cohort level because the paper explicitly claims QSVM.
      - Add a note that the circuit body is an ansatz_stretch proxy rather than a QSVM feature-map/kernel circuit.

      Optional reauthoring for stronger family fidelity:
      - Replace ansatz_stretch with a QSVM kernel proxy.
      - Use a small feature-map circuit such as ZZFeatureMap or another explicit quantum feature map.
      - Implement a fidelity-kernel structure using feature_map(x) followed by feature_map(y)^dagger and measurement, or a SWAP-test-style kernel estimator.
      - Keep n_qubits=5 unless a later source provides the actual feature count/qubit encoding.
      - Continue to mark the paper as underspecified because it does not report enough implementation detail for exact circuit reconstruction.

      Aggregation guidance:
      - Include SQ12 only in template-proxy or weak-faithfulness aggregates.
      - Exclude it from any headline claim about resource costs of concrete QSVM kernel circuits unless reauthored with an explicit QSVM feature-map/kernel template.
```
