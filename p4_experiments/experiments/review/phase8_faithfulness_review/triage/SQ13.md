# Triage: SQ13

**Reviewers (joint):** Vincent Wallerich + Aleix Telesforo  
**Triage date:** 2026-04-25  
**Vincent verdict (first pass):** **DRIFT_MINOR**

---

## Paper

- **Title:** Quantum State Preparation by Improved MPS Method
- **Paper ID:** `59cca9744140` · **Experiment:** `exp_1` · **Silo:** `quantum-ml-finance` · **Cohort algorithm family:** `other-gate-based`

**Sources to consult:**
- Paper markdown: [p2_systematic_review/output/processed/59cca9744140.md](p2_systematic_review/output/processed/59cca9744140.md)
- Circuit: [p4_experiments/experiments/silos/quantum_ml_finance/59cca9744140/circuit.py](p4_experiments/experiments/silos/quantum_ml_finance/59cca9744140/circuit.py)
- Instance: [p4_experiments/experiments/silos/quantum_ml_finance/59cca9744140/instance.json](p4_experiments/experiments/silos/quantum_ml_finance/59cca9744140/instance.json)
- Phase 3 LLM extraction (GPT 5.3 / GPT 5.4-mini, P3 s2_quantitative): [p3_thematic_synthesis/s2_quantitative/output/extractions/59cca9744140.json](p3_thematic_synthesis/s2_quantitative/output/extractions/59cca9744140.json) (experiment_id `exp_1`)
- Vincent's review: [../vincent/SQ13.md](../vincent/SQ13.md)

---

## Disagreement

Vincent's review (first pass) flagged this label as **DRIFT_MINOR** —
i.e. the implemented circuit does not faithfully match what the
Phase 3 LLM extraction (GPT 5.3 / GPT 5.4-mini) recorded the paper's algorithm to be.

**Vincent's verdict summary:** Generic RealAmplitudes ansatz template proxy at 5 qubits for a 16-qubit IMPS state-preparation paper; family bucket matches and proxy use is the explicit methodology, but circuit-internal labels say SQ12 while the cohort entry is SQ13.

## Phase 3 (LLM) extraction — values for this label

**Source:** [p3_thematic_synthesis/s2_quantitative/output/extractions/59cca9744140.json](p3_thematic_synthesis/s2_quantitative/output/extractions/59cca9744140.json) (experiment_id `exp_1`)  
**Models used (per source `extraction_metadata.models_used`):** `gpt-5.3` (synthesis) + `gpt-5.4-mini` (extraction steps).

| Field | Phase 3 value |
|---|---|
| `algorithm_family` | `other-gate-based` |
| `algorithm_variant` | `Improved MPS (IMPS) state preparation / amplitude encoding` |
| `num_qubits` | `6` |
| `ansatz` (free-text) | `MPS/TTN/HTN/HEN tensor-network-inspired state-preparation circuits` |
| `num_layers` | _(not extracted in P3 S2)_ |
| `circuit_depth` | `11` |
| `oracle_structure` | _(not extracted in P3 S2)_ |
| `encoding` | _(not extracted in P3 S2)_ |

_Note: P3 s2_quantitative extracts paper-level algorithm and quantum-resource fields. Circuit-level details that are not in the S2 schema (e.g. `oracle_structure`, `encoding`) appear as _(not extracted in P3 S2)_ above. Vincent's per-check notes below adjudicate those fields from direct paper + circuit reading._

### Vincent's per-check notes

| Check | Result | Note |
|---|---|---|
| Family-faithful | ✅ | Both paper and circuit are tagged algorithm_family='other-gate-based'. Paper is custom MPS/TTN/HTN amplitude-encoded state preparation with KAK-based 2-CNOT decomposition; circuit is a generic ansatz template proxy. Within the explicitly permitted template-proxy methodology, the family bucket is preserved. |
| Scale-faithful | ❌ | Paper reports up to ~16 qubits; circuit runs at n_qubits=5. Ratio is 3.2x (>factor of 2) and 5 is well below the 12-qubit tractability cap, so scale is not at the maximum allowed. Acceptable as an intentional small-scale stretch but flagged. |
| Structurally non-trivial | ✅ | Bare circuit is RealAmplitudes(n=5, reps=3, entanglement='linear') with seeded parameters; full variant adds ansatz . ansatz^dagger compute-uncompute and measurement. Non-trivial as a generic state-prep proxy, though it does not contain SVD/MPS/HTN structure or the KAK 2-CNOT decomposition that defines the paper's algorithm. |
| Metadata-consistent | ❌ | circuit.py registers label='SQ12' (in @register, register_accounting, names 'SQ12_bare'/'SQ12_full', and module docstring), and instance.json has instance_id='SQ12_v3_proxy_v1', label='SQ12'. Cohort/task label_id is SQ13. Paper_id (59cca9744140), silo, and algorithm_family align, but the SQ12/SQ13 label mismatch is a real metadata inconsistency. |

### Concerns raised by Vincent

- circuit.py and instance.json both label this artifact 'SQ12' while the cohort task is SQ13
- circuit is a generic RealAmplitudes ansatz; it does not implement IMPS, SVD-derived 4th-order unitaries, TTN/HTN connectivity, or the KAK 2-CNOT decomposition that are the paper's core contributions
- n_qubits=5 is below the 12-qubit tractability cap and not within a factor of 2 of the paper's 16
- U_depth values from paper (1,3,5,10,11) are not reflected; ansatz_layers=3 is a generic proxy depth
- paper is a methods/state-prep paper (operator note flags 'methods-only, not finance application'), so any downstream Phase-8 cost extrapolation should treat this cohort with caution

### Recommendations from Vincent

- Rename label='SQ12' -> 'SQ13' in circuit.py @register, register_accounting, circuit names, and docstring; rename instance.json label/instance_id accordingly to align with the cohort label_id
- Consider lifting n_qubits toward the 12-qubit cap to better cover the paper's 16-qubit regime, or document the 5-qubit choice as an explicit stretch in instance.json notes
- If feasible, add a minimal MPS/TTN-shaped block (e.g., staircase of 2-qubit unitaries) so the proxy at least exhibits the paper's tensor-network connectivity pattern instead of pure RealAmplitudes
- Flag this cohort as 'methods-only / not finance application' in the Phase-8 cost aggregator to avoid biasing finance-task resource estimates

---

## DECISION (joint manual review)

```yaml
label_id: SQ13
paper_id: 59cca9744140
experiment_id: exp_1
reviewers: [Vincent Wallerich, Aleix Telesforo]
reviewed_utc: 2026-04-25

# winner: 'phase3' | 'vincent' | 'merge' | 'other'
# action:  'accept_vincent' | 'reauthor_circuit' | 'demote_label' | 'relabel_family' | 'no_change'
decision:
  winner: vincent
  action: accept_vincent
  rationale: |
    Accept Vincent's DRIFT_MINOR assessment.

    Phase 3 is faithful to the paper at the algorithm level: the paper proposes an Improved Matrix Product State (IMPS) method for quantum state preparation / amplitude encoding. The method reduces the number of fourth-order unitaries compared with canonical MPS preparation, introduces improved connectivity structures such as TTN, HTN, and HEN, and uses KAK-style decomposition to reduce fourth-order unitaries to two CNOT gates plus single-qubit rotations.

    The implemented circuit described in Vincent's review is a generic RealAmplitudes ansatz-stretch proxy with compute-uncompute accounting. This is not structurally faithful to the paper's IMPS method: it does not implement SVD-derived MPS disentangling, TTN/HTN/HEN connectivity, fourth-order unitary blocks, or the two-CNOT KAK decomposition that defines the paper's contribution.

    However, the drift is minor rather than major because the implementation is explicitly a template proxy, and both the paper and cohort sit in the broad other-gate-based bucket. The circuit is non-empty and gate-rich enough to serve as a weak resource proxy, but it should not be interpreted as a faithful IMPS/MPS state-preparation implementation.

    The main concrete issue is metadata consistency. The pasted review indicates that circuit.py and instance.json identify this artifact as SQ12, while the cohort and audit item identify it as SQ13. This must be fixed before downstream accounting.

    Scale is also weak: the paper evaluates up to 16 qubits, while the proxy uses 5 qubits. This is below the 12-qubit tractability cap and outside a factor-of-2 comparison to 16. It is acceptable only as a documented small-scale stretch.
  evidence_section: >
    PDF page 1, Abstract and Introduction: the paper presents an improved MPS method for quantum state preparation, aimed at efficient amplitude encoding and reduced circuit depth for quantum machine-learning state preparation.
    PDF page 3, Section 2.1 and Figure 1: the paper describes canonical MPS preparation and shows a 6-qubit MPS circuit using five fourth-order unitary matrices.
    PDF pages 4-5, Section 2.1 and Figures 2-3: the paper introduces improved disentangling/connectivity structures, including TTN and HTN-style schemes, to reduce circuit depth and avoid strict nearest-neighbor constraints.
    PDF page 6, Section 2.2: the paper proves that the fourth-order unitary matrix arising from SVD in MPS amplitude preparation can be decomposed into two CNOT gates and single-qubit rotations.
    PDF pages 8-12, Numerical Experiments and Figures 4-9: the paper evaluates MPS, TTN, HTN, and HEN amplitude-preparation methods across several functions/distributions and qubit counts up to 16, including chip-adapted experiments with 12 qubits and depths around 5 and 10.
    PDF page 13, Discussion and Conclusion: the paper concludes that the IMPS framework enables efficient quantum state preparation with O(log n) circuit depth under bounded-rank assumptions and can reduce hardware-related errors by optimizing circuit depth for arbitrary topologies.
  remediation:
    template: ansatz_stretch
    n_qubits: 5
    notes: |
      Required immediate fixes:
      - Rename all SQ12 references in circuit.py to SQ13.
      - Rename instance.json label and instance_id from SQ12 to SQ13.
      - Align @register(label=...), register_accounting(label=...), circuit names, function names, module docstring, and description with SQ13.
      - Keep paper_id=59cca9744140 and experiment_id=exp_1.

      Faithfulness status:
      - Keep SQ13 as DRIFT_MINOR / template-proxy.
      - Do not treat the current RealAmplitudes circuit as a faithful IMPS/MPS implementation.
      - Mark it as an ansatz_stretch resource proxy for an IMPS state-preparation paper.

      Optional stronger remediation:
      - Replace ansatz_stretch with an MPS/TTN-shaped proxy.
      - Use a staircase or tree-like sequence of two-qubit unitaries to mimic the paper's MPS/TTN/HTN connectivity.
      - Include explicit two-CNOT blocks plus single-qubit rotations to reflect the paper's KAK decomposition result.
      - Consider increasing n_qubits from 5 toward the 12-qubit tractability cap, since the paper evaluates regimes up to 16 qubits and also reports chip-adapted 12-qubit experiments.
      - If n_qubits remains 5, document this as an intentional small-scale stretch rather than a scale-faithful reproduction.

      Aggregation guidance:
      - Include SQ13 only in template-proxy or weak-faithfulness aggregates.
      - Exclude it from any headline claim about faithful IMPS / MPS / tensor-network state-preparation resource estimates unless reauthored with an MPS/TTN-style circuit.
      - Flag the paper as methods-only / not a finance application, so it does not overweight finance-task-specific resource conclusions.
      
```
