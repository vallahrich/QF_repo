# Derivative Pricing

Authoritative per-paper experiment evidence for the derivative_pricing silo. Each subfolder is one experiment, named `<hash>` or `<hash>__exp_N` for variant runs; per-experiment metadata (`instance.json`, `circuit.py`, `notes.md`, `proxy_justification.md`, etc.) lives inside.
