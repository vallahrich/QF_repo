# Fraud Detection

Authoritative per-paper experiment evidence for the fraud_detection silo. Each subfolder is one experiment, named `<hash>` or `<hash>__exp_N` for variant runs; per-experiment metadata (`instance.json`, `circuit.py`, `notes.md`, `proxy_justification.md`, etc.) lives inside.
