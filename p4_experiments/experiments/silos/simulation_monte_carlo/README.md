# Simulation / Monte Carlo

Authoritative per-paper experiment evidence for the simulation_monte_carlo silo. Each subfolder is one experiment, named `<hash>` or `<hash>__exp_N` for variant runs; per-experiment metadata (`instance.json`, `circuit.py`, `notes.md`, `proxy_justification.md`, etc.) lives inside.
