# Minimal pinned dependencies for examiner verification of the frozen archive.
#
# Scope: just enough to run
#   pwsh ./verify.ps1
#   python -m pytest
#
# These pins match p4_experiments/canonical/requirements.lock so the verify
# subset stays consistent with the heavy Phase 4 lock. They do NOT include
# LLM, quantum, or Phase 4 runtime dependencies; for those, install
# p4_experiments/canonical/requirements.lock instead.
#
# Setup:
#   python -m venv .venv
#   .\.venv\Scripts\Activate.ps1        # Windows PowerShell
#   # source .venv/bin/activate         # POSIX
#   pip install -r requirements-verify.txt

jsonschema==4.26.0
numpy==2.3.4
openai==1.109.1
openpyxl==3.1.5
pandas==2.3.3
pytest==9.0.1
python-dotenv==1.2.1
python-frontmatter==1.1.0
