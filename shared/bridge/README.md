# `shared/bridge/` — Canonical paper-ID bridge

| File | Purpose |
|------|---------|
| `paper_id_bridge.csv` | Canonical mapping between SLR `paper_id` hashes, DOIs, and Zotero item keys. **Used by all four phases.** |
| `excluded_papers.csv` | Excluded-paper register feeding PRISMA + post-hoc triage. |

See parent [`../README.md`](../README.md).