# `shared/phase3/` — P3-derived shared assets

P3-derived inputs that are reused by other phases / by the manuscript.

| Subfolder | Purpose |
|-----------|---------|
| `bibliometric/` | Bibliometric summaries (corpus / silo / method / year). |
| `inclusion/` | Per-silo inclusion lists (one JSON per active silo). |
| `text_readiness/` | Per-silo text-readiness reports (one JSON per active silo). |

See parent [`../README.md`](../README.md).